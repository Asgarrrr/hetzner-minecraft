# Discord Bot

This directory is reserved for the future Discord bot.

When we add it, the idea is to keep here:

- `Dockerfile`
- `package.json`
- `src/`
- any bot-specific config files

The bot will be able to reach Minecraft on the Docker host `minecraft` as long as it runs in the same Compose project.
