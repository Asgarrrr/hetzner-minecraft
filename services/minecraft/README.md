# Minecraft Service

This directory is reserved for files specific to the Minecraft server:

- service documentation
- backup/restore scripts
- configuration overrides if we add them later

The server's runtime data does not live here. It stays in `../../volumes/minecraft/`.
